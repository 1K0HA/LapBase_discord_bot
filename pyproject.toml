[project]
name = "lapbase"
version = "1.0.2"
description = "Discord news -> Groq translation -> Telegram publishing pipeline"
requires-python = ">=3.14,<3.15"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py314"
